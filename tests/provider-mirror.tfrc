provider_installation {
  filesystem_mirror {
    # Path from subfolder
    path    = "../../artifacts/provider-mirror"
    include = ["nexgencloud/*"]
  }

  direct {
    exclude = ["nexgencloud/*"]
  }
}
